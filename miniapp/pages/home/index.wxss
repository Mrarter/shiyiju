.home-page {
  min-height: 100vh;
  background: linear-gradient(180deg, #160d0a 0%, #0f0907 100%);
  color: #f6eee8;
}

.home-wrap {
  padding: 36rpx 26rpx 42rpx;
}

.home-header {
  padding: 6rpx 10rpx 24rpx;
}

.brand {
  font-size: 38rpx;
  color: #dcc2a1;
  font-weight: 600;
}

.subtitle {
  margin-top: 18rpx;
  font-size: 66rpx;
  line-height: 1.2;
  font-weight: 700;
}

.desc {
  margin-top: 16rpx;
  font-size: 28rpx;
  color: #b7a493;
}

.hero-card,
.headline-card,
.summary-card,
.banner-pill,
.artist-card,
.hot-card,
.fresh-card,
.rise-card,
.state-card,
.empty-state {
  background: #1b1310;
  border: 1rpx solid rgba(77, 53, 42, 0.85);
  border-radius: 30rpx;
  box-sizing: border-box;
}

.hero-card {
  overflow: hidden;
}

.banner-strip {
  display: flex;
  gap: 16rpx;
  overflow-x: auto;
  padding-bottom: 8rpx;
  margin-bottom: 18rpx;
}

.banner-pill {
  flex: 0 0 320rpx;
  padding: 20rpx 22rpx;
  background: linear-gradient(135deg, rgba(53, 34, 24, 0.98), rgba(27, 19, 16, 0.98));
}

.banner-eyebrow {
  color: #d8b06a;
  font-size: 22rpx;
}

.banner-title {
  margin-top: 12rpx;
  font-size: 30rpx;
  font-weight: 700;
}

.banner-subtitle {
  margin-top: 10rpx;
  color: #b7a493;
  font-size: 24rpx;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.headline-strip {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 18rpx;
  margin-bottom: 22rpx;
}

.headline-card {
  padding: 24rpx 18rpx;
  background: linear-gradient(180deg, rgba(42, 28, 21, 0.98), rgba(28, 20, 16, 0.98));
}

.headline-value {
  font-size: 42rpx;
  line-height: 1;
  font-weight: 700;
  color: #e2bb79;
}

.headline-label {
  margin-top: 12rpx;
  font-size: 24rpx;
  color: #b7a493;
}

.hero-image-wrap {
  padding: 24rpx 24rpx 0;
}

.hero-image,
.hot-image,
.fallback-art {
  width: 100%;
  display: block;
  border-radius: 22rpx;
}

.hero-image,
.fallback-art {
  height: 360rpx;
}

.hero-body {
  padding: 22rpx 28rpx 28rpx;
}

.hero-chip {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-width: 110rpx;
  height: 48rpx;
  padding: 0 18rpx;
  border-radius: 999rpx;
  background: #2a1d17;
  color: #e7c899;
  font-size: 24rpx;
}

.hero-title {
  margin-top: 28rpx;
  font-size: 56rpx;
  line-height: 1.2;
  font-weight: 700;
}

.hero-meta,
.summary-desc,
.hot-meta,
.rise-meta,
.rise-status,
.empty-desc {
  font-size: 28rpx;
  color: #b7a493;
}

.hero-meta {
  margin-top: 14rpx;
}

.hero-insight {
  display: flex;
  flex-wrap: wrap;
  gap: 12rpx;
  margin-top: 20rpx;
}

.hero-insight-item,
.hot-tag,
.fresh-tag {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-height: 44rpx;
  padding: 0 18rpx;
  border-radius: 999rpx;
  background: rgba(255, 255, 255, 0.06);
  color: #dcc2a1;
  font-size: 22rpx;
}

.hero-bottom,
.module-head,
.rise-card,
.fresh-card,
.hero-actions,
.summary-grid {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.hero-bottom {
  margin-top: 26rpx;
}

.hero-price,
.hot-price,
.rise-price {
  color: #d8b06a;
  font-size: 50rpx;
  line-height: 1;
  font-weight: 700;
}

.hero-status {
  font-size: 26rpx;
  color: #b7a493;
  margin-top: 22rpx;
}

.summary-grid {
  gap: 18rpx;
  margin-top: 22rpx;
}

.summary-card {
  flex: 1;
  padding: 22rpx 24rpx;
  background: #241915;
}

.summary-title,
.module-title {
  font-size: 38rpx;
  font-weight: 700;
}

.summary-desc,
.module-count {
  margin-top: 10rpx;
}

.module {
  margin-top: 34rpx;
}

.artist-scroll {
  width: 100%;
  white-space: nowrap;
}

.artist-row {
  display: inline-flex;
  gap: 18rpx;
  padding-bottom: 4rpx;
}

.artist-card {
  width: 218rpx;
  padding: 18rpx;
  box-sizing: border-box;
}

.artist-avatar {
  width: 100%;
  height: 180rpx;
  border-radius: 20rpx;
  display: block;
}

.artist-name {
  margin-top: 16rpx;
  font-size: 30rpx;
  font-weight: 700;
}

.artist-level {
  margin-top: 8rpx;
  font-size: 22rpx;
  color: #dcc2a1;
}

.artist-slogan {
  margin-top: 10rpx;
  font-size: 22rpx;
  color: #b7a493;
  line-height: 1.5;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

.module-head {
  margin-bottom: 18rpx;
  padding: 0 6rpx;
}

.module-count {
  font-size: 24rpx;
  color: #968575;
}

.hot-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 18rpx;
}

.hot-card {
  padding: 18rpx;
}

.hot-image,
.hot-image-wrap .fallback-art {
  height: 136rpx;
}

.hot-title,
.rise-title,
.empty-title {
  margin-top: 18rpx;
  font-size: 34rpx;
  font-weight: 700;
}

.hot-meta {
  margin-top: 8rpx;
  font-size: 24rpx;
}

.hot-price {
  margin-top: 10rpx;
  font-size: 42rpx;
}

.hot-tags {
  display: flex;
  flex-wrap: wrap;
  gap: 10rpx;
  margin-top: 12rpx;
}

.hot-tag.subtle {
  color: #b7a493;
}

.fresh-list {
  display: flex;
  flex-direction: column;
  gap: 14rpx;
}

.fresh-card {
  padding: 22rpx 24rpx;
}

.fresh-main {
  flex: 1;
  min-width: 0;
}

.fresh-side {
  margin-left: 20rpx;
  text-align: right;
}

.fresh-title {
  font-size: 34rpx;
  font-weight: 700;
}

.fresh-meta {
  margin-top: 10rpx;
  font-size: 24rpx;
  color: #b7a493;
}

.fresh-price {
  color: #e2bb79;
  font-size: 36rpx;
  font-weight: 700;
}

.fresh-tag {
  margin-top: 12rpx;
}

.rise-list {
  display: flex;
  flex-direction: column;
  gap: 14rpx;
}

.rise-card {
  padding: 22rpx 28rpx;
}

.rise-main {
  flex: 1;
  min-width: 0;
}

.rise-side {
  margin-left: 18rpx;
  text-align: right;
}

.rise-title,
.hot-title,
.fresh-title,
.hero-title,
.hot-meta,
.rise-meta,
.fresh-meta {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.rise-title {
  margin-top: 0;
}

.rise-meta,
.rise-status {
  margin-top: 8rpx;
  font-size: 24rpx;
}

.fallback-art {
  position: relative;
  overflow: hidden;
}

.art-glow {
  position: absolute;
  left: 64rpx;
  top: 48rpx;
  width: 208rpx;
  height: 208rpx;
  border-radius: 50%;
  opacity: 0.95;
}

.art-glow.small {
  left: 30rpx;
  top: 20rpx;
  width: 104rpx;
  height: 104rpx;
}

.art-mountain {
  position: absolute;
  bottom: 0;
  width: 320rpx;
  height: 220rpx;
  opacity: 0.86;
  clip-path: polygon(0 100%, 48% 18%, 100% 100%);
}

.art-mountain.left {
  left: 300rpx;
}

.art-mountain.center {
  left: 520rpx;
  height: 160rpx;
}

.art-mountain.right {
  right: -20rpx;
}

.art-mountain.small {
  width: 120rpx;
  height: 76rpx;
}

.art-mountain.left.small {
  left: 110rpx;
}

.art-mountain.center.small {
  left: 190rpx;
  height: 58rpx;
}

.art-mountain.right.small {
  right: -10rpx;
}

.art-line {
  position: absolute;
  left: 64rpx;
  right: 120rpx;
  top: 170rpx;
  height: 8rpx;
  background: #ebcba7;
  transform: rotate(-13deg);
  transform-origin: center;
}

.art-line.small {
  left: 20rpx;
  right: 52rpx;
  top: 68rpx;
  height: 5rpx;
}

.state-card,
.empty-state {
  padding: 32rpx 28rpx;
}

.hero-actions {
  justify-content: flex-start;
  gap: 18rpx;
  margin-top: 24rpx;
}

.hero-action,
.retry-btn {
  height: 82rpx;
  padding: 0 28rpx;
  border-radius: 22rpx;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  font-size: 28rpx;
  font-weight: 700;
  background: rgba(255, 255, 255, 0.06);
  color: #f6eee8;
}

.hero-action.primary,
.retry-btn {
  background: linear-gradient(135deg, #d5b06a, #a67c33);
  color: #120f0d;
}

.retry-btn {
  margin-top: 20rpx;
}
